% Standalone build for Figure 2. Compile with pdflatex; the resulting
% PDF has MediaBox = CropBox = the artwork box (no page padding).
\documentclass[border=0pt]{standalone}
\usepackage{times}
\usepackage{streamweave-figures}
\begin{document}
\input{figure2}
\end{document}
